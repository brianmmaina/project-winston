# For ``npm run dev`` (Vite dev server). In production / docker-compose the
# build sets ``VITE_API_URL=""`` so the SPA uses relative URLs through nginx.
VITE_API_URL=http://localhost:8000

# Mirror of the backend's ``API_KEY``. Leave empty to skip auth in local dev.
VITE_API_KEY=
